Flask==3.0.3
Flask-CORS==4.0.2
Flask-SocketIO==5.3.7
Flask-Limiter==3.8.0
python-socketio==5.11.4
python-dotenv==1.0.1
psutil==6.0.0
pyautogui==0.9.54
pynput==1.7.7
pyperclip==1.9.0
GPUtil==1.4.0
pywin32==311; sys_platform == 'win32'
# Absolute volume get/set for the slider control — pure-Python COM wrapper,
# no external tools. Optional at runtime: volume_set falls back to NirCmd.
pycaw==20240210; sys_platform == 'win32'
PyJWT==2.9.0
bcrypt==4.2.1
python-magic-bin==0.4.14; sys_platform == 'win32'
Pillow>=10.4.0
Werkzeug==3.0.6
requests==2.32.3
pytz==2024.2

# Optional: only needed for the "Ask Claude (API)" action, which calls the
# Anthropic Messages API directly. The Claude Code CLI actions do not need it,
# and the pack greys that one action out with an install hint when it is
# missing, so VDock works fine without this.
#   pip install anthropic
# anthropic>=0.40.0
